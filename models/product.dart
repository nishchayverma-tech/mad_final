class Product {
  String? id;
  String? name;
  double? price;
  String? photoUrl;
  String? description;
  int? quantity;

  Product(this.id, this.name, this.price, this.photoUrl, this.description, this.quantity);
}